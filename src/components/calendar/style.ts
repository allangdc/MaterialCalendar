import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  headercell: {
    border: "solid",
    borderWidth: 1,
    minHeight: 24,
  },
  cell: {
    border: "solid",
    borderWidth: "0.01px",
  },
});
